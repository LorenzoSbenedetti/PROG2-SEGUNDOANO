#include <stdio.h>
#include <stdlib.h>
#define N 5
int busca(int *v){
    int i,numero;
    scanf("%d",&numero);
    for(i=0;i<N;i++){
        if (v[i] == numero){
            return 1;
        }
    }
    return 0;
}
int main()
{
int v[N];
int i;
for(i=0;i<N;i++){
    scanf("%d",&v[i]);
    }
printf("%d",busca(v));
}
