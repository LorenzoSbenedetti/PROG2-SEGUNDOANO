#include <stdio.h>
#include <stdlib.h>
int soma (int n){
    if (n <= 1){
        return n;
    }
    else {
        return 2*soma(n-1)+ soma(n-2);
    }
}
int main()
{
    int i,n;
    scanf("%d",&n);
    printf("%d\n", soma(n));
}
